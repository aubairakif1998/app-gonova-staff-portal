/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["api.microlink.io", "firebasestorage.googleapis.com"], // Add your image domains here
  },
};

export default nextConfig;
